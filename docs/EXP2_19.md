# EXP2.19: Schedule-Locked Outer-Momentum Policy

## Question

Can a lower-memory outer optimizer improve long-horizon asynchronous Qwen3.5-9B
training more reliably than candidate selection or static fragment-ID learning
rates?

EXP2.18 established two important facts:

1. The original endpoint comparisons were contaminated by disconnect-driven
   partial tail commits and unequal outer-step counts.
2. Once every arm is locked to the same 80 full-quorum outer steps, the static
   fragment profile is only about `0.001` better than its fixed mean-LR control.

The next hypothesis is therefore about trajectory-level optimizer memory, not
which candidate should be accepted.

## Motivation

The tuned baseline uses Nesterov momentum `0.9` for every RDA fragment. A
geometry audit over controlled seed-101 and seed-113 captures found that, after
momentum builds up:

```text
history-term norm / current-term norm: about 0.70
applied-step norm / current merged-update norm: about 2.23
cosine(applied step, current merged update): about 0.80
```

The outer step is therefore strongly influenced by historical trajectory
memory and is rotated away from the current merged learner evidence. This is a
plausible cause of the late-stage loss increase seen in the common-step
trajectories.

The first policy tested is deliberately simple:

```text
baseline:
  outer momentum = 0.9
  outer LR = 0.175

damped-memory policy:
  outer momentum = 0.5
  outer LR = 0.221667
```

The candidate LR approximately matches the baseline's first-step Nesterov
gain:

```text
0.175 * (1 + 0.9) / (1 + 0.5) = 0.221667
```

This policy retains every candidate, adds no communication or probe overhead,
and does not use oracle labels.

## Controlled Protocol

All runs use the exact-step mode added in commit `7ae4281`:

```text
strict configured quorum = 4
no partial timeout commits
no disconnect-driven quorum reduction
fixed outer-step count
every fragment receives the same number of commits
post-run event-tape validation
```

Shared setup:

| Item | Value |
|---|---|
| Model | `qwen35-9b` |
| Data | `trl-lib/Capybara` |
| Learners | 4, one A100 per learner |
| Fragments | 4 all-RDA `binpack` fragments |
| Local window | 8,192 tokens / 64 optimizer steps |
| Merge | HeLoCo plus RDA |
| Inner LR | 0.001 |
| LoRA rank / alpha | 2 / 4 |
| Pipeline | 2 |
| Delay pattern | 0/800/1600/2400 ms after frozen snapshots |
| External holdouts | two disjoint 256-row Capybara subsets |

## Development Screen

The first screen used a fresh seed and approximately 200k training tokens:

```text
shuffle seed: 137
training seed: 737373
outer steps: 24
commits per fragment: 6
learner ceiling: 550 steps
```

Both arms had identical initial-state and first-window candidate hashes. Both
event tapes contained exactly 24 groups, and every group had all four
responders.

| Evaluation | Momentum 0.9, LR 0.175 | Momentum 0.5, LR 0.221667 | Damped minus baseline |
|---|---:|---:|---:|
| Internal 64-row eval | 1.3988 | 1.3934 | -0.0054 |
| External holdout A | 1.389845 | 1.381847 | -0.007998 |
| External holdout B | 1.389379 | 1.382909 | -0.006470 |
| Wall time | 466 s | 477 s | +11 s |

The development gate passes: the damped-memory policy improves the internal
split and both external holdouts by more than `0.005`, with about 2.4% wall
time difference in this short run.

## Full-Budget Confirmation

The selected policy is frozen before confirmation:

```text
outer momentum = 0.5
outer LR = 0.221667
outer steps = 80
learner ceiling = 1600 for seeds 149/163
```

No momentum or LR tuning is allowed on confirmation seeds.

### Seed 149

```text
shuffle seed: 149
training seed: 849849
outer steps: 80
full-quorum groups: 80/80 for both arms
```

The initial syncer checkpoint and all four first-window candidate tensors
matched exactly between arms.

| Evaluation | Momentum 0.9, LR 0.175 | Momentum 0.5, LR 0.221667 | Damped minus baseline |
|---|---:|---:|---:|
| Internal 64-row eval | 1.4356 | 1.3951 | -0.0405 |
| External holdout A | 1.432700 | 1.391431 | -0.041269 |
| External holdout B | 1.436437 | 1.395241 | -0.041196 |
| Wall time | 1,504 s | 1,509 s | +5 s |

Seed 149 is a strong first full-budget confirmation. The improvement is about
`0.041` on two independently materialized external holdouts, with only 0.3%
wall-time difference. The result is much larger than the development-seed
gain, so another untouched full-budget seed is required before calling the
policy stable.

### Seed 163

Seed 163 was launched after all seed-149 training and holdout results were
fixed. It uses shuffle seed `163` and training seed `963963`; no policy
parameters were changed.

Both arms again completed exactly 80 full-quorum groups and began from
identical initial-state and first-window candidate hashes.

| Evaluation | Momentum 0.9, LR 0.175 | Momentum 0.5, LR 0.221667 | Damped minus baseline |
|---|---:|---:|---:|
| Internal 64-row eval | 1.3904 | 1.3497 | -0.0407 |
| External holdout A | 1.426981 | 1.387235 | -0.039746 |
| External holdout B | 1.431063 | 1.392627 | -0.038436 |
| Wall time | 1,517 s | 1,491 s | -26 s |

Seed 163 independently replicates the seed-149 result. The two full-budget
confirmations both improve by about `0.04` on both external holdouts, while
the candidate is not slower in aggregate.

### Seed 177

A third untouched full-budget confirmation uses shuffle seed `177` and
training seed `177777`. It was queued before reading seed-163 results and uses
the same frozen momentum and LR values.

The first attempt used the original 1,600-step learner ceiling. One fast
learner disconnected at outer step 78, so strict quorum could no longer reach
80. That attempt is preserved under directories ending in
`failed-max1600` and is excluded. The clean retry changed only the
nonbinding learner ceiling to 2,500 steps. Both arms then completed exactly
80 full-quorum groups, with 20 commits per fragment. Their initial syncer
checkpoint and all four first-window candidates matched byte-for-byte.

| Evaluation | Momentum 0.9, LR 0.175 | Momentum 0.5, LR 0.221667 | Damped minus baseline |
|---|---:|---:|---:|
| Internal 64-row eval | 1.4802 | 1.4372 | -0.0430 |
| External holdout A | 1.430978 | 1.390644 | -0.040334 |
| External holdout B | 1.433052 | 1.395191 | -0.037861 |
| Wall time | 1,525 s | 1,574 s | +49 s |

Seed 177 is a third independent confirmation. Two additional diagnostic
evaluations agree with the main holdouts:

| Diagnostic evaluation | Momentum 0.9 | Momentum 0.5 | Damped minus baseline |
|---|---:|---:|---:|
| Third disjoint 256-row holdout | 1.420502 | 1.381100 | -0.039402 |
| 64-row materialization of holdout A | 1.373500 | 1.335039 | -0.038461 |

The training, strict-quorum tape, checkpoint, and adapter export completed
normally. A post-training orchestration mistake launched extra diagnostic
evals on the same four-GPU blocks before the harness's internal eval. The
harness timed out waiting for those GPUs, so the internal metric was rerun
from the immutable exported adapters. The original traceback is preserved as
`run.original.log`; `recovery_summary.json` records the recovery. Wall time
was reconstructed from learner-log timestamps using the offset that exactly
reproduces the recorded wall times of seeds 149 and 163.

### Three-Seed Aggregate

Positive improvement means the damped pair has lower loss.

| Metric | Mean improvement | Seeds positive |
|---|---:|---:|
| Internal eval | 0.041400 | 3/3 |
| External holdout A | 0.040450 | 3/3 |
| External holdout B | 0.039164 | 3/3 |
| Mean of both external holdouts | 0.039807 | 3/3 |
| Mean wall-time change | +0.61% | n/a |

All six runs contain exactly 80 contiguous event-tape groups, all groups have
responders `{0,1,2,3}`, and every fragment receives 20 commits. The frozen
pair therefore passes the predeclared replication gate by a wide margin.

This establishes the joint optimizer setting, not momentum as an isolated
cause. The treatment simultaneously shortens memory and reduces the
constant-direction gain:

```text
baseline DC gain:  0.175 / (1 - 0.9) = 1.750000
treatment DC gain: 0.221667 / (1 - 0.5) = 0.443334
```

The same momentum buffer is also used by HeLoCo before the Nesterov step.
Factorial, DC-gain, and no-HeLoCo controls are therefore required before a
mechanistic claim.

## Decision Gate

Treat damped outer memory as promising only if the full-budget confirmations:

```text
improve both external holdouts on at least 2/3 fresh seeds
have mean gain >= 0.005
have no seed regression > 0.010
agree in sign on internal eval and both holdouts
retain exactly 80 full-quorum groups
add no material systems overhead
```

One positive development seed is not sufficient for a policy claim.

Outcome: `PASS`. The treatment wins the internal split and both external
holdouts on all three untouched full-budget seeds, with mean two-holdout gain
`0.039807` and mean wall-time change `+0.61%`.

## Causal Controls

The following controls were queued before interpreting momentum as causal:

| Control | Arm A | Arm B | Purpose |
|---|---|---|---|
| 2x2 missing corners, seed 137 | `mu=0.9, lr=0.221667` | `mu=0.5, lr=0.175` | Separate the LR main effect from the momentum main effect and their interaction. |
| DC-gain match, seed 181 | `mu=0.9, lr=0.044333` | `mu=0.5, lr=0.221667` | Match `lr/(1-mu)` and test whether shorter memory helps beyond steady-state scalar gain. |
| HeLoCo deconfound, seed 191 | `mu=0.9, lr=0.175` | `mu=0.5, lr=0.221667` | Repeat with `--delta-correction none` so both policies stop feeding different momentum buffers into HeLoCo. |

The first two controls use 24 strict full-quorum steps and 200k tokens. The
no-HeLoCo control uses 40 strict full-quorum steps and 350k tokens. No online
adaptive policy claim is made until these controls finish.

### Seed-137 2x2 Factorial

The two missing corners completed 24/24 strict full-quorum groups. Combined
with the original development pair, the complete factorial is:

| Momentum | Outer LR | Internal | Holdout A | Holdout B | Mean holdout |
|---:|---:|---:|---:|---:|---:|
| 0.9 | 0.175000 | 1.3988 | 1.389845 | 1.389379 | 1.389612 |
| 0.9 | 0.221667 | 1.4045 | 1.395155 | 1.395415 | 1.395285 |
| 0.5 | 0.175000 | **1.3918** | **1.380945** | **1.382556** | **1.381751** |
| 0.5 | 0.221667 | 1.3934 | 1.381847 | 1.382909 | 1.382378 |

At fixed LR `0.175`, lowering momentum from `0.9` to `0.5` improves the two
holdouts by `0.008900` and `0.006823`. At fixed LR `0.221667`, the same
momentum change improves them by `0.013308` and `0.012506`. Conversely,
raising LR hurts at both momentum values, by a mean holdout `0.005673` when
`mu=0.9` and `0.000627` when `mu=0.5`.

This short controlled factorial points to a real lower-momentum main effect,
not a higher-LR explanation. It also identifies `(mu=0.5, lr=0.175)` as the
best short-run corner. The effect remains a single-seed, 24-step result and
still shares the HeLoCo momentum buffer, so a full-budget confirmation and
the no-HeLoCo control remain necessary.

## Exact Command Shape

```bash
python scripts/compare_diloco.py \
  --model qwen35-9b \
  --data trl-lib/Capybara \
  --settings m4 \
  --outer-lr OUTER_LR \
  --outer-momentum OUTER_MOMENTUM \
  --token-budget 700000 \
  --seq-len 128 \
  --micro-batch-size 1 \
  --inner-lr 0.001 \
  --lora-r 2 \
  --lora-alpha 4 \
  --eval-rows 64 \
  --max-rows 5000 \
  --shuffle-rows-seed SEED \
  --training-seed TRAINING_SEED \
  --device cuda \
  --learner-gpus 1 \
  --fixed-window-tokens 8192 \
  --fixed-window-microsteps 64 \
  --pad-to-fixed-window-tokens \
  --freeze-delta-before-delay \
  --learner-push-delay-ms 0,800,1600,2400 \
  --learner-delay-jitter-ms 50 \
  --syncer-total-steps 80 \
  --learner-max-steps LEARNER_CEILING \
  --strict-quorum \
  --syncer-probe-capture \
  --syncer-probe-capture-every 1 \
  --work-dir RUN_DIR/work \
  --report-dir RUN_DIR/report
```

## Artifact Paths

```text
experiment-results/EXP2/qwen9b-locked-mu09-seed137-200k/
experiment-results/EXP2/qwen9b-locked-mu05-seed137-200k/
experiment-results/EXP2/qwen9b-locked-mu09-seed149-700k/
experiment-results/EXP2/qwen9b-locked-mu05-seed149-700k/
experiment-results/EXP2/qwen9b-locked-mu09-seed163-700k/
experiment-results/EXP2/qwen9b-locked-mu05-seed163-700k/
experiment-results/EXP2/qwen9b-locked-mu09-seed177-700k/
experiment-results/EXP2/qwen9b-locked-mu05-seed177-700k/
experiment-results/EXP2/qwen9b-outer-momentum-summary.json
experiment-results/EXP2/qwen9b-outer-momentum-summary.md
```
