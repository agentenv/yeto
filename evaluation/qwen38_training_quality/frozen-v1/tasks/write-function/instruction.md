Create solution.py with a function total_even(values) that returns the sum of even integers in values. It must handle an empty list and negative integers.

The working directory is /work/quality/write-function. Complete the requested file change, check the result, and report completion.
