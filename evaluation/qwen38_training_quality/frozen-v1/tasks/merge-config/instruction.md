Merge inputs/base.json and inputs/override.json into result.json. Override keys replace base values; preserve other base keys.

The working directory is /work/quality/merge-config. Complete the requested file change, check the result, and report completion.
