Copy inputs/settings.txt to result.txt while replacing only the line mode=old with mode=new. Preserve all other bytes.

The working directory is /work/quality/patch-text. Complete the requested file change, check the result, and report completion.
