Read the JSON object in inputs/values.json. Compute (left + right) * scale and write the decimal value plus a newline to result.txt.

The working directory is /work/quality/evaluate-expression. Complete the requested file change, check the result, and report completion.
