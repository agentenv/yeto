Compute the SHA256 of the exact bytes in inputs/payload.txt; write its lowercase hex digest plus a newline to result.txt.

The working directory is /work/quality/checksum-file. Complete the requested file change, check the result, and report completion.
