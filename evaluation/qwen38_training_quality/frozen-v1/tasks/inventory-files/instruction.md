List every regular file below inputs/tree, recursively, using relative paths from inputs/tree. Sort the paths and write one per line to result.txt.

The working directory is /work/quality/inventory-files. Complete the requested file change, check the result, and report completion.
