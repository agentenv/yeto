"""Provider-neutral AgentEnv provision contracts and solver.

This package is intentionally independent of Yeto's training stack. It must
never import ``yeto.cli``, ``yeto.shape``, Torch, Transformers, boto, or Sky.
"""
