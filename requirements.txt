# Learner-node dependencies. transformers is pinned for DeepSeek-V4-Flash
# compatibility (its remote code is incompatible with transformers 5.x).
# torch is NOT listed here: the wheel must match the node's driver AND the
# GPU generation (Blackwell/SM100 needs cu128, which needs driver >=570;
# Ampere/Hopper AMIs ship driver 535, whose ceiling is cu121-era wheels
# and which lacks SM100 kernels either way). The learner task setup
# installs the right torch FIRST (see TORCH_SETUP in yeto/launcher.py),
# so pip resolves the packages below against it instead of dragging in a
# default wheel.
transformers==4.57.1
datasets>=3.0
peft>=0.14
accelerate>=1.0
sentencepiece
protobuf
hf_transfer
cloudpickle
