# Outer-muP v9 7B pre-outcome scope reduction

Status: **registered, outcome-blind, and effective before any valid 7B completion**.

At the inventory snapshot updated at 2026-07-28 00:53:55Z, the four 7B slot-state records each said `state=RUNNING`, `queue_index=0`, and `completed=0`; the registered queues therefore contained four running and eight queued cells. The only 7B evidence `status` values read were three attempt-1 `INFRA_FAILURE` records. There was no `COMPLETED` evidence status and the sum of the four slot `completed` counters was zero. Cell state was never inferred from directory existence. The full paths, snapshot hashes, state fields, queue positions, and evidence status records are frozen in `outer-mup-v9-7b-scope-reduction.json`.

The sealed predictions are untouched at raw SHA-256 `97e02dcad63782978ac51b320621e5a681236518cb0d5db19454b8981549ca9c`. They name both `mu0` and `raw` and three verification etas per arm. A quadratic needs all three eta rungs, so the smallest symmetric evaluable reduction is one complete seed panel: seed 907 at eta indices 0, 1, and 2 for both arms. This retains six cells and removes all six seed-901 cells symmetrically.

The mandatory prospective simulation reuses the original frozen G9B curvature and noise priors, Gaussian draw order, quadratic fit, 0.5-bit near-bracket allowance, shared 10,000-draw bootstrap sequence, 7,500-valid-refit threshold, and 0.5-bit arm bands. A singleton rung is its own empirical bootstrap distribution. Across 5,000 centered-null simulations, the six-cell panel has `P_eval=0.8250` and `P_pass_given_evaluable=0.872727`, both above the mandatory 0.8 bar. It is therefore selected without adding cells back. The committed simulation is `outer-mup-v9-7b-scope-reduction-gatesim.json`, raw SHA-256 `cc0bea6d6444e39e140ed3e26a580116c50cc67ad1210ee8b019ecd4154d4d3d`.

The amended analyzer still validates the complete original 28-cell launch manifest and the original sealed-prediction hash, but loads scientific evidence only for all 16 registered 1.7B cells and the six retained 7B cells. For G9B it fits the registered quadratic to seed 907 at all three sealed etas for each arm. The original bootstrap draw sequence is preserved; resampling a singleton empirical seed panel maps every draw to seed 907. Six valid 7B `COMPLETED` evidence records and accepted fits for both targets remain mandatory—no missing retained result may be dropped.

The live manifest remains historical launch provenance bound to registration commit `1001572d`; attempt-2 executes at descendant `57711ba`, which contains barrier fix `084d58c` and retry-provenance hardening. This overlay changes scope without rewriting that record. Removed running work may be terminated only by its exact scientific PID after resolving the cell from its `RUNNING` slot-state; bracketed `pgrep` is verification-only. Removed attempt directories are parked as `attempt-2-descoped` under an unchanged-mtime guard. Completed trees and the sealed predictions are immutable, and freed GPUs belong to the tonight85 lane.
