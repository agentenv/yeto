# TONIGHT-8.5 LEAN overnight registration

This directory freezes four prospective studies for the extended two-node H200 lease:

- v11: ratio-transport at never-measured SmolLM2-135M/T=80 and SmolLM2-1.7B/T=40 coordinates;
- v12: the same three-horizon scan under classical heavy-ball outer momentum without Nesterov lookahead;
- v13: the Nesterov scan on EleutherAI/pythia-160m and a deterministic HuggingFaceH4/ultrachat_200k shard;
- v7: a pre-outcome reduction to the registered 27B-LoRA pilot and a four-cell descriptive raw spot check.

The machine-readable JSON contracts are authoritative. `tonight85-gatesim.json` records pre-outcome measurement-noise/bracketing feasibility, not scientific evidence. V11 and reduced-v7 use conditional prediction seals: their exact numeric prediction artifacts must be committed and pushed from the Mac after the registered pilot/anchor and before the corresponding ground-truth cells start.

Execution is loss-blind and fleet-gated. The live v9 7B wave has absolute priority. No tonight process may start merely because the clock passes 20:30; both nodes must have empty bracketed priority-controller checks and no GPU compute PID. The short v12/v13 queues interleave first, v11 follows, and 27B-LoRA runs last. A gate not completely drained and analyzed by 08:30 America/Los_Angeles is labeled `DEFERRED_ARXIV_V2`, never rushed or partially promoted.
